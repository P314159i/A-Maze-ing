[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v"
pythonpath = ["."]

[tool.mypy]
strict = true
exclude = [
    ".venv",
    "__pycache__",
    ".pytest_cache",
]

[tool.flake8]
exclude = [
    ".venv",
    "__pycache__",
    ".pytest_cache",
    ".git",
]
